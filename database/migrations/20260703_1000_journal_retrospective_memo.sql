-- 20260703_1000_journal_retrospective_memo.sql
-- target: backend
-- ============================================================================
-- 저널 회고 메모 (outcome 확정 후 복기 기록)
-- ----------------------------------------------------------------------------
-- 배경: 저널의 user_memo 는 "작성 시점 판단"의 기록이다. 7/30거래일 outcome 이
--   확정된 뒤 "예상과 달랐던 점"을 남기는 회고는 별도 필드로 분리해야 판단→결과→
--   회고의 복기 루프가 완성된다. API 는 outcome 이 1건 이상 확정된 저널에만 회고
--   작성을 허용한다(400 RETROSPECTIVE_NOT_READY).
-- ============================================================================

ALTER TABLE public.signal_journals
    ADD COLUMN IF NOT EXISTS retrospective_memo text;
