"""Vendored volatility-benchmark models + point-in-time DataContract.

Source: biop99999111/vol-benchmark (version-pinned vendoring). Only import-path
adjustments are made; model logic is preserved verbatim so cross-model results
stay comparable. Heavy model backends are NOT imported here — import the specific
model module you need so the base install stays numpy/pandas only.
"""
