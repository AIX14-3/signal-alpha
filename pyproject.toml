[project]
name = "signal-alpha"
version = "0.1.0"
description = "Signal Alpha monorepo workspace"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "httpx>=0.27.0",
  "psycopg2-binary>=2.9.0",
  "pytest>=8.0.0",
  "ruff>=0.14.0",
]
# Streamlit dev/ops 도구(services/agent-worker/script/dashboard_validator.py · db_explorer.py).
# 런타임 서비스와 무관해 기본(dev) 그룹과 분리. 설치: `uv sync --all-packages --group dashboard`
# (⚠️ --all-packages 없으면 워크스페이스 멤버 런타임 deps(selenium/torch 등)가 제거됨).
dashboard = [
  "streamlit>=1.40.0",
  "pandas>=2.0.0",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = [
  "packages/data-access",
  "packages/market-data",
  "packages/signal-core",
  "packages/vol-models",
  "services/agent-worker",
  "services/main-server",
]

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = [
  ".venv",
]

[tool.ruff.lint]
select = [
  "E",
  "F",
]
ignore = [
  "E501",
]

[tool.ruff.lint.per-file-ignores]
"*/tests/*.py" = [
  "E402",
]
# 스크립트는 import 전에 sys.path/.env/logs 부트스트랩이 필요해 E402가 정상이다.
"*/script/*.py" = [
  "E402",
]
